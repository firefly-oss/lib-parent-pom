name: lib-parent-pom CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-publish:
    uses: catalis-vc/cicd/.github/workflows/lib-workflow.yml@main
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}